# 任务：中文开发意图分级与四对话同步

- 状态：completed
- 日期：2026-07-14
- 对话：00-历史开发归档（开发效率规范）
- 分支：`docs/development-intent-routing`
- 基线：`5e12e9a57dc83ff138bdcaaf3d7915e35f9f83f4`
- 固定 App 来源：纯文档流程任务，不改变安装包
- 负责人：Codex（本地执行）

## 用户问题

用户希望不必说技术命令，代理应从自然中文判断是在快速开发预览、运行相关模块测试、正式更新固定 App，还是制作对外发布包；涉及整体开发的核心诉求必须默认同步给 Bug、新功能、UI 和工程发布四个专用对话。

## 目标

- 明确“每条反馈”和“完整任务完成”的区别，避免每次微调都正式打包。
- 建立中文自然表达与开发阶段的默认映射，有歧义时优先快速预览。
- 明确相关模块测试是开发代理的默认责任，不要求用户主动索要。
- 将跨项目核心规则同步责任写入代理入口，并通知四个专用对话。

## 非目标

- 本任务不实现新的开发服务器、热重载脚本或增量打包器。
- 不修改产品运行时代码、正式 App 或用户数据。

## 影响范围

- 模块：开发流程、任务意图路由、跨对话同步。
- 文件：`AGENTS.md`、`docs/DEVELOPMENT_WORKFLOW.md`、ADR-0003、本验收单。
- 数据格式：无。
- 平台：Codex 开发流程。

## 验收条件

- [x] 用户可用自然中文明确四种阶段，不必记 npm 命令。
- [x] 有歧义时默认快速开发预览，不擅自正式打包。
- [x] 每个完整用户可见任务仍只在最终验收时正式交付一次。
- [x] 项目级核心指令的四对话同步责任已经写入仓库并实际通知。
- [x] `npm run test:foundation` 和 `npm run test:i18n` 通过。
- [x] 纯文档任务不更新固定 App，并在验收记录说明。

## 测试计划

- 最小命令：`npm run test:foundation`、`npm run test:i18n`。
- 升级到全量的条件：若修改任何运行时代码或 package scripts。
- 固定 App 交付：不适用；只改变开发协作规则。

## 实施记录

- 假设：用户使用“继续调、先看看、检查一下、可以了、发布版本”等普通中文表达。
- 关键决定：任务过程中可多次快速预览；相关模块测试默认随开发运行；只有明确进入验收完成才正式打包一次。
- 实际修改：在 `AGENTS.md` 与开发流程中增加中文意图四级路由、歧义默认预览、任务不等于单条消息、最终只交付一次，以及项目核心规则默认同步四个专用对话；ADR-0003 同步记录交付频率边界。

## 验证结果

| 命令/步骤 | 结果 | 耗时 | 备注 |
| --- | --- | ---: | --- |
| `npm run test:foundation` | 通过 | <1s | 仓库基础 58 项，命令内同时执行国际化守卫。 |
| `npm run test:i18n` | 通过 | <1s | 21 项通过；没有修改运行时翻译资源。 |
| 四个专用对话同步 | 通过 | <1min | Bug、新功能、UI、工程发布均明确回复规则立即生效。 |
| 固定 App 交付 | 不适用 | — | 本任务只改变开发协作文档，没有改变安装包或用户可见行为。 |

## 交接

- 规则提交：`1b0e291`
- 验收归档：本文件所在提交
- PR：无（未连接 GitHub）
- 工作区状态：完成时干净
- 下一步：后续用户照常用自然中文表达，代理主动声明所选阶段并执行对应流程
