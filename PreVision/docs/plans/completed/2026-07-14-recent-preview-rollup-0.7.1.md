# 任务：近期 Bug 预览正式收口与本机 App 更新

- 状态：completed
- 日期：2026-07-14
- 对话：历史开发归档/主协调对话
- 分支：fix/recent-preview-rollup-0.7.1
- 基线：50c091086c18dd7c42414437f85225ba58219987
- 固定 App 来源：5e12e9a57dc83ff138bdcaaf3d7915e35f9f83f4
- 负责人：Codex

## 用户问题

把近期多次 Bug 修复的预览版全部正式写入本机 App 完成更新，版本号自动更新。

## 目标

- 集成近期已在开发预览中完成并验证的桌面端改动。
- 按用户在 Bug 专用对话中的插队要求，补齐全身主要关节、四个常用场景/构图模板和剧本到分镜同步。
- 自动将应用版本从 0.7.0 递增到 0.7.1。
- 完成全量验证，并更新固定入口 `~/Applications/PreVision.app`。

## 非目标

- 不纳入尚未定稿的 Web 首页静态概念稿。
- 不制作对外分发的 ZIP、DMG，不进行签名或公证。

## 证据与现状

- 代码：近期预览成果集中在 Bug 修复专用工作树，包含画布外观、马模型、沙漠贴地和场景镜头层级改动。
- Git：固定 App 来源为 `5e12e9a`；本分支包含该提交，当前基线为 `50c0910`。
- 测试/运行：各预览轮次已运行相关模块测试；正式收口需在集成提交上重新运行全量测试。
- 文档/历史线索：以 Bug 工作树的验收单、代码和可重复测试结果为准。

## 影响范围

- 模块：actor、background、project、storyboard、layout、display、desktop、i18n。
- 文件：主应用 HTML、语言包、QA 登记、测试、Electron 版本元数据及相关文档。
- 数据格式：无预期破坏性变更；需验证旧项目可继续加载。
- 平台：macOS Apple Silicon。

## 风险

- 数据：多轮预览叠加可能引入旧项目兼容回归。
- UI/交互：移除底部重复镜头条后，镜头入口必须在左栏完整可用。
- 安全：不得把其他工作树、构建产物或本机信息带入提交。
- 发布：必须由唯一协调分支执行固定 App 交付，防止旧分支覆盖。

## 验收条件

- [x] 已完成的近期预览改动全部进入单一可追溯分支，未定稿的 Web 探索不混入。
- [x] 全身主要关节、四个场景/构图模板和剧本到分镜同步完成并有回归覆盖。
- [x] 版本号自动递增为 0.7.1，应用包元数据一致。
- [x] 国际化、主应用全量、桌面端与本地安装测试通过。
- [x] 目标尺寸人工验证完成。
- [x] 用户可见任务已执行 `npm run app:deliver`，并从固定 App 看到本次变化。
- [x] 文档和功能登记已更新。

## 测试计划

- 影响映射模块：actor、background、project、storyboard、layout、display、desktop、i18n。
- 主应用模块参数：actor / background / project / storyboard / layout / display。
- 最小命令：`npm run test:i18n`、`npm run test:desktop`、`npm run test:app`。
- 升级到全量的条件：本任务跨越主应用多个模块，直接运行 `npm run test:full`。
- 人工检查尺寸/步骤：固定 App 在笔记本常用尺寸和 1440px 级宽度检查左栏镜头层级、画布外观、沙漠贴地与马模型。
- 固定 App 交付：需要；目标路径固定为 `~/Applications/PreVision.app`。

## 实施记录

- 假设：近期预览成果与用户随后明确插入的三项扩展需求一起构成 0.7.1 的交付范围。
- 关键决定：由主协调分支统一集成和交付；其他专用对话不得更新固定 App。2026-07-14 用户在 Bug 专用对话明确要求先补做三项扩展再提交干净树，本轮据此等待完整回归后再交付。
- 实际修改：安全整合画布外观 checkpoint 与场景创作升级提交；新增全身主要关节、四模板及剧本分镜路由；纳入白马头部统一、连续沙漠/骑乘贴地、项目→场景→镜头层级和底栏去重；使用 npm patch 版本规则把 0.7.0 自动递增为 0.7.1，并同步更新日志、产品状态和 QA 登记。

## 验证结果

| 命令/步骤 | 结果 | 耗时 | 备注 |
| --- | --- | ---: | --- |
| `npm run app:status` | 通过 | <1s | 固定 App 来源 `5e12e9a`；本分支包含该来源 |
| `npm run test:i18n` | 通过 | <1s | 21 通过，0 失败；双语言 key 对齐且无新增运行时内联中文 |
| `npm run test:app` | 通过 | 约 10s | 436 通过，0 失败 |
| `npm run test:desktop` | 通过 | <1s | 23 通过，0 失败 |
| `npm run test:full` | 通过 | 约 18s | 应用 436、桌面 23、安装事务 36、交付门禁 13、基础 58、国际化 21，全部 0 失败 |
| `npm run app:deliver` | 通过 | 正式交付 | Node 24 构建 macOS arm64，更新并启动唯一固定入口 |
| 固定 App 人工验收 | 通过 | 人工 | 核对左栏层级、画布三态、白马/沙漠/骑乘、全身关节、四模板和剧本分镜入口 |

固定 App installed source：本验收单所属最终提交（由 `app:status` 校验 installed/current 精确一致）

固定 App 人工启动结果：`~/Applications/PreVision.app` 自动打开，0.7.1 核心界面和本轮新增入口可见。

## 未覆盖与后续

- 未定稿 Web 首页概念稿继续保留在独立 UI 工作树，不进入本次桌面 App。

## 交接

- 最终提交：本验收单所属最终提交
- PR：无
- 工作区状态：干净
- 下一步：用户继续通过四个专用任务提交后续需求；下一次正式更新仍只由统一交付分支执行一次。
