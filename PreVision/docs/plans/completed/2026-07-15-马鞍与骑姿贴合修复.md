# 任务：马鞍与骑姿贴合修复

- 状态：completed
- 日期：2026-07-15
- 对话：01.x｜马鞍与骑姿贴合修复
- 分支：fix/rider-saddle-fit
- 基线：d28eaf58a261edea86c62d74772d2eeacb73c74b
- 固定 App 来源：7ff9aa583b4e51fb4d888aa1815792b747d275d7（chore/preview-rollup-0.7.2）
- 负责人：Codex

## 并行任务声明

- 任务 ID：01.x-rider-saddle-fit
- 模式：write
- 模块：actor, project
- UI 表面：viewport, inspector
- 数据区域：actor-rig, project-v5
- 实际修改文件：`预见PreVision.html`、`测试/冒烟测试.mjs`、`docs/FEATURE_REGISTRY.md`、`qa/feature-registry.yaml`、本验收单、已完成索引及 `docs/qa/rider-saddle-fit/`
- `task:check` 结果：无硬冲突；接管收尾时按实际证据文件扩展范围后复查仍为 `No hard conflicts`
- `task:claim`：`01.x-rider-saddle-fit` 已登记；接管时以相同逻辑范围和扩展后的实际文件重新登记
- `task:release`：实现提交完成后已执行，结果 `RELEASED 01.x-rider-saddle-fit`；共享登记不进入 Git

## 用户问题

从最新集成基线重新救回历史未完成的“低马鞍 + 骑乘姿态贴合”成果，不带回其他旧改动。

## 目标

- 马鞍贴合马背，骑手挂载后呈可信坐姿。
- 保持上下马、路径、朝向、贴地、碰撞和 project v5 往返语义。
- 保持语义代理与当前 `buildActor` 分支能力，尺寸异常时安全近似。

## 非目标

- 不带回 capture、缩略图、欢迎场景、摄影机实体等旧工作树改动。
- 不调用真实付费 AI，不公开部署，不执行 `app:deliver` 或更新固定 App。

## 证据与现状

- 代码：基线白马仍使用较厚椭球鞍体与半圆 Torus 鞍桥；全局通用骑姿同时服务白马、车辆及普通物体挂载。
- Git：基于总协调提交 `d28eaf5` 新建独立分支。
- 测试/运行：Node 24.14.0；`app:status` 已确认包含固定 App 来源。
- 文档/历史线索：旧工作树仅只读提取相关 diff。

## 影响范围

- 模块：actor, project
- 文件：主应用、聚焦回归、验收证据及必要登记
- 数据格式：无版本升级；保护 project v5 兼容
- 平台：浏览器与 Electron 共用导演台运行时

## 风险

- 数据：挂载偏移或尺寸派生值误入持久化会破坏旧项目往返。
- UI/交互：极端人物/马匹比例只能提供有界视觉近似。
- 安全：不新增 IPC、文件权限或外部服务。
- 发布：仅开发预览，不更新固定 App。

## 验收条件

- [x] 马鞍位置与马背贴合，默认白马骑手呈可信坐姿。
- [x] 挂载/解除、朝向、路径所有权、贴地、碰撞和 project v5 往返保持现有语义。
- [x] 语义代理 `semanticType`、`dimensions`、未知类型降级及当前 `buildActor` 分支保持。
- [x] 合理尺寸调整有效，极端尺寸不崩溃、不写坏数据。
- [x] 摄影机监视器、捕获和 Seedance 输出不出现调试对象。
- [x] 相关自动测试通过。
- [x] 1440×900 左右开发预览人工验证并保留截图证据。
- [x] 本任务按明确要求不执行 `app:deliver`，不更新固定 App。
- [x] 文档和功能登记已更新。

## 测试计划

- 影响映射模块：actor, project
- 主应用模块参数：actor / project
- 最小命令：`npm run test:module -- actor`、`npm run test:module -- project`、`npm run test:i18n`、`npm run test:app`、`npm run test:impact -- --base d28eaf5`
- 升级到全量的条件：跨模块语义或影响测试提示，或回归风险无法由聚焦断言覆盖
- 人工检查尺寸/步骤：约 1440×900；空马、默认骑手、尺寸调整、保存重开、卸载/重新挂载
- 固定 App 交付：不适用；用户明确禁止更新 `~/Applications/PreVision.app`

## 实施记录

- 假设：挂载视觉偏移保持运行时派生，不改变 project v5 schema。
- 关键决定：保留通用 `POSE_JOINTS.ride` 原值，新增仅对白马宿主生效的贴鞍预设；旧值迁移放在当前场景所有对象构建完成之后，避免 actor 保存顺序影响宿主识别。
- 实际修改：鞍毯/鞍体缩窄压薄并下沉，前后鞍桥由半圆 Torus 改为低矮椭球；白马骑手使用抬高 0.1m、髋外展 ±46°、膝 72° 的默认值。旧 project v5 只逐字段迁移仍等于历史白马默认的值，用户自定义关节不变；车载和普通物体挂载保持历史默认。
- 范围守卫：没有带回旧工作树的 capture、缩略图、欢迎场景或摄影机实体改动。

## 验证结果

| 命令/步骤 | 结果 | 耗时 | 备注 |
| --- | --- | ---: | --- |
| `npm run app:status` | 通过 | <1s | 当前分支包含固定 App 来源 `7ff9aa5`；固定 App 未更新 |
| `npm run task:check -- ...` / `task:claim -- ...` | 通过 | <1s | 无硬冲突；实际证据文件已纳入 claim |
| `npm run task:release -- --task 01.x-rider-saddle-fit` | 通过 | <1s | `RELEASED`；随后 `task:status` 为无活动 claim |
| `npm run test:module -- actor` | 130 通过，0 失败 | 约 3s | 白马专用姿态、车/普通物体守卫、缩放、运动与鞍面净空 |
| `npm run test:module -- project` | 27 通过，0 失败 | 约 4s | rider-before-horse、幂等、自定义/语义字段、非马挂载往返 |
| `npm run test:i18n` | 21 通过，0 失败 | <1s | 未增加用户文案或直接中文 |
| `npm run test:app` | 580 通过，0 失败 | 约 18s | 完整主应用回归 |
| `npm run test:impact -- --base d28eaf5... --module actor --module project` | 通过 | 约 20s | app 580；foundation 81 + coordination 20 + i18n 21；web 10 + stress 13 |
| `npm run test:full` | 全部通过 | 约 20s | app 580、web 10+13、desktop 43、local-install 36+13、foundation 81+20+21 |
| `npm run web:build` | 通过 | <1s | provided-home，19 files |
| 回环 Web 人工检查 | 通过 | — | 1440×900 验收视口；空马、默认/缩放骑手、卸载重挂、刷新重开，证据见 `docs/qa/rider-saddle-fit/` |

固定 App installed source：7ff9aa583b4e51fb4d888aa1815792b747d275d7

固定 App 人工启动结果：不适用；本任务禁止交付。

## 未覆盖与后续

- 真实付费 Seedance 服务不在自动验证范围。
- 极端马/人物比例只保证有限、稳定、可往返的挂载近似，不提供人体或鞍具 IK 自动重塑。

## 交接

- 最终提交：本任务聚焦提交；精确哈希见总协调交接消息
- PR：无（仓库无 remote）
- 工作区状态：提交后 clean
- 下一步：完成后由 00 按顺序集成。
