# 任务：04.4｜24 对象压力夹具引用修复

- 状态：completed
- 日期：2026-07-16
- 对话：固定 00 派发的侧栏可见短期临时工
- 分支：`test/web-stress-fixture-reference-repair`
- 基线：`a9472e3ae4638728eae845fae6d482e2557d3282`
- 固定 App 来源：`7ff9aa583b4e51fb4d888aa1815792b747d275d7` (`chore/preview-rollup-0.7.2`)；当前分支包含该来源但不精确相等；本任务不交付固定 App
- 负责人：Codex 短期工程临时工

## 并行任务声明

- 任务 ID：`04.4-web-stress-fixture-reference-repair`
- 模式：write
- 模块：`testing`
- UI 表面：无
- 数据区域：`qa-metadata`
- 预计修改文件：`scripts/web-stress-lib.mjs`、`测试/Web压力测试工装测试.mjs`、本验收单与 `docs/plans/completed/README.md`
- `task:check` 结果：无硬冲突；与 04.5 仅 `docs/plans/completed/README.md` 软冲突，集成顺序为 04.4 先、04.5 后
- `task:claim`：已登记
- `task:release`：未释放，由 00 集成后处理

## 用户问题

24 对象/4 场景合成压力夹具替换 actors 后，镜头仍锁定不存在的旧 A/B 标签；产品安全归一化正确拒绝它，导致压力流程可能回落默认项目并给出假结论。

## 目标

- 修复 synthetic stress fixture 的全部引用完整性。
- 加载后 fail-closed 确认运行时确为 4 场景、每场景 24 对象、预期镜头/角色标识且零 alert。
- 增加回归，证明旧 A/B 悬空引用会被工装自身识别，新夹具可被产品接受。

## 非目标

- 不改 `预见PreVision.html`，不放宽 normalize/openProject 安全边界。
- 不改非法引用拒绝、事务回滚或提示语义。
- 不改四平台矩阵、GPU 分类、证据去敏、私有 CDP 或清理契约。

## 证据与现状

- 代码：待审计 `scripts/web-stress-lib.mjs` 的合成项目构造与加载。
- Git：HEAD 精确为指定基线，已创建独立 `test/` 分支。
- 测试/运行：Node 24 `task:status` 初始无 claim；`app:status` 因当前 Worktree 尚未安装 `@electron/asar` 而失败，待 `npm ci` 后复核。
- 文档/历史线索：`CURRENT_STATE` 记录现有 Web 压力工装 12 项契约测试。

## 影响范围

- 模块：`testing`
- 文件：仅压力工装、其测试与验收文档
- 数据格式：无产品数据变更；仅 synthetic project v5 测试夹具
- 平台：Web 压力工装，契约覆盖既有四平台矩阵

## 风险

- 数据：若验证不够严格，仍可能在默认项目上跑压测。
- UI/交互：无产品 UI 修改。
- 安全：不得为夹具通过而削弱产品引用校验。
- 发布：纯测试工装任务，不运行 `app:deliver`。

## 验收条件

- [x] 合成镜头仅使用实际存在的合成角色标签。
- [x] 所有 camera lock、同步/路径引用均由工装自身校验。
- [x] 加载后 fail-closed 断言 4 场景 × 24 对象、预期镜头/角色标识与零 alert。
- [x] 旧 A/B 悬空引用被工装自身拒绝，新夹具被真 Chrome 中的产品加载流程接受。
- [x] 既有 Web 压力契约无回归。
- [x] Node 24 下 `test:web`、`test:foundation`、`test:i18n`、`test:impact --base a9472e3...` 通过。
- [x] 真 Chrome smoke 人工观察步骤完成，使用工装自选的临时回环端口，未占用 4175。
- [x] 固定 App 交付不适用：本任务只改测试工装。
- [x] 验收单已 completed 并归档/索引。

## 测试计划

- 影响映射模块：`web-stress`、`foundation`
- 主应用模块参数：无，不改业务 HTML
- 最小命令：`npm run test:web`、`npm run test:foundation`、`npm run test:i18n`
- 升级到全量的条件：影响映射要求或修改逸出工装范围。
- 人工检查尺寸/步骤：可在独立临时端口执行一次真 Chromium 最小检查，不占用 4175。
- 固定 App 交付：不适用。

## 实施记录

- 假设：只要工装在导入后严格识别夹具的结构和标识，便不会将默认项目误认为压力项目。
- 关键决定：为每个合成镜头设置当场景真实 actor label；导入前校验原始夹具，严格检查 `openProjectData(...) === true`和零 alert，再校验产品归一化后的运行时项目。
- 实际修改：新增可同时在 Node 契约测试和页面 bootstrap 内执行的 synthetic fixture 校验器；校验场景/对象/镜头标识、lock、syncActor、mount、timeLinkShot 和路径数值；历史实测证据改为对其记录的 `sourceCommit` 验证 harness 哈希，不伪改历史证据。

## 验证结果

| 命令/步骤 | 结果 | 耗时 | 备注 |
| --- | --- | ---: | --- |
| Node 24 `npm run app:status` | 失败（前置） | <1s | Worktree 未安装 `@electron/asar`，`npm ci` 后复核 |
| Node 24 `npm ci` | 通过 | 9s | 504 packages；已有 audit 报告 23 项，本任务不改依赖 |
| Node 24 `npm run app:status` | 通过 | <1s | contains installed source: yes；exact: no |
| Node 24 `npm run test:web` | 通过 | <2s | Web 底座 10/10；压力工装 14/14 |
| Node 24 `npm run web:stress:check` | 通过 | <1s | Chrome 150 可用；Safari 仍为未授权；Windows 仍明确阻塞 |
| Node 24 `npm run web:stress -- --browser chrome --profile smoke --attestation unattested --output dist/web-stress-evidence/04-4-fixture-smoke.json` | 通过 | 约 20s | 9/9；typical 4×24、0 alert；cleanup passed；无 crash/context lost；不计入矩阵证据 |
| Node 24 `npm run test:foundation` | 通过 | 约 2s | foundation 93、coordination 31、i18n 21、project-input-wrapper 11 全通过 |
| Node 24 `npm run test:i18n` | 通过 | <1s | 21/21 |
| Node 24 `npm run test:impact -- --base a9472e3ae4638728eae845fae6d482e2557d3282` | 通过 | 约 4s | 命中 foundation + web-stress，全部通过 |

固定 App installed source：`7ff9aa583b4e51fb4d888aa1815792b747d275d7`；本任务不更新它。

固定 App 人工启动结果：不适用。

## 未覆盖与后续

- 本次 Chrome smoke 使用 `unattested`，只证明当前真浏览器最小流程，不替代 macOS/Windows 四平台正式矩阵。
- 产品安全边界由既有业务测试保护，本任务未修改 `预见PreVision.html`。
- `npm ci` 报告的已有依赖漏洞不在本次夹具修复范围内。

## 交接

- 最终提交：本验收单所在的聚焦提交
- PR：无 remote
- 工作区状态：提交后 clean（交接前复核）
- 下一步：00 安全集成后 release claim，并按 04.4 先、04.5 后处理索引软冲突。
