# 任务：03.9｜时间轴播放头与关键帧命中优先级

- 状态：completed
- 日期：2026-07-16
- 对话：03.9｜时间轴播放头与关键帧命中优先级
- 分支：fix/03.9-timeline-playhead-hit-priority
- 基线：62e20fe29066c4fa83d6354e5baacd8b099d2e2e
- 固定 App 来源：7ff9aa583b4e51fb4d888aa1815792b747d275d7
- 负责人：Codex 独立短期临时工

## 并行任务声明

- 任务 ID：03.9-timeline-playhead-hit-priority
- 模式：write
- 模块：timeline, testing
- UI 表面：timeline
- 数据区域：shot-camera, object-paths, qa-metadata
- 预计修改文件：`预见PreVision.html`、`测试/冒烟测试.mjs`、`测试/项目输入DOM探针.cjs`、本验收单、`docs/plans/completed/README.md`、`docs/qa/timeline-playhead-hit-priority/` 下的去敏证据
- `task:check` 结果：无冲突
- `task:claim`：已登记
- `task:release`：未释放（由 00 集成后释放）

## 用户问题

播放头的透明命中条在相同 x 坐标遮挡关键帧、关键帧组和 clip grip，导致真实浏览器拖动错误进入 scrub；需要用时间轴局部 stacking 顺序修复，并补足真实布局与 pointer drag 证据。

## 目标

- 关键帧/关键帧组、clip grip、播放头命中条依次获得明确的局部交互层级。
- 真 Chrome 在 1316×768 与 1440×900 覆盖 `elementFromPoint` 和真实 pointer drag，同时验证空白 lane/ruler 仍可 scrub。
- 回归 pointercancel、lost capture 和错误 pointerId 的既有会话语义。

## 非目标

- 不缩窄播放头命中条，不禁用其 pointer events，不增加 JS 事件转发。
- 不改时间轴数据、插值、history/autosave 或无关时间轴结构。
- 不处理弹窗命令、Seedance 提示词复制、移动端、录制或资源生命周期。
- 不运行 `app:deliver`，不更新固定 App 或稳定预览 4175。

## 证据与现状

- 代码：`#motionPlayhead` 命中条 z-index 8；clip grip 与 legacy/preview key/group 位于更低层级。
- Git：HEAD 精确为任务基线，且基线包含固定 App 来源。
- 测试/运行：`npm run app:status` 确认 Contains installed source 为 yes、Exact 为 no。
- 文档/历史线索：当前无其他 active 验收单；以本任务委派中的只读复现为线索，最终以代码和真浏览器复现为准。

## 影响范围

- 模块：timeline
- 文件：主应用 CSS/时间轴交互测试、任务验收与真浏览器 QA 证据
- 数据格式：无
- 平台：浏览器与 Electron 共用渲染页面；真 Chrome 验证 macOS 目标尺寸

## 风险

- 数据：不应产生项目、history 或 autosave 写入。
- UI/交互：层级过高可能遮挡相邻控制，需同时验证空白 scrub 和各类拖动。
- 安全：无新增 IPC、外部服务或不可信输入面。
- 发布：仅开发预览，不打包、不安装固定 App。

## 验收条件

- [x] legacy key、preview child key、preview group key、clip grip 与播放头同 x 时命中自身并完成真实拖动。
- [x] 空白 lane/ruler 保留播放头 scrub；命中条宽度和 pointer events 不变。
- [x] pointercancel、lost capture、错误 pointerId 的既有语义通过回归。
- [x] timeline、robustness、国际化、影响测试与全量测试通过。
- [x] 1316×768 与 1440×900 真 Chrome 证据完成并去敏。
- [x] 固定 App 交付不适用：本任务明确仅开发预览，由 00 集成后统一交付。
- [x] QA 文档已更新；TIME-001～003 功能语义和 VERIFIED 状态未变，无需修改功能登记。

## 测试计划

- 影响映射模块：main-app、app-test、foundation
- 主应用模块参数：timeline、robustness
- 最小命令：`npm run test:module -- timeline`、`npm run test:module -- robustness`、`npm run test:i18n`
- 升级到全量的条件：主应用单体 HTML 或冒烟测试发生任何修改（本任务必然升级）
- 人工检查尺寸/步骤：真 Chrome 1316×768 与 1440×900，逐类 `elementFromPoint`、真实 pointer drag、空白 lane/ruler scrub
- 固定 App 交付：不适用；本任务禁止更新 `~/Applications/PreVision.app`

## 实施记录

- 假设：只需局部 CSS stacking 修复，事件会话代码保持不变；真 Chrome 验证证明该假设成立。
- 关键决定：在 `#motionRows` 的既有局部 positioning/stacking 范围内使用 key/group z10、grip z9、playhead z8；不改命中尺寸、pointer events 或 JS 分发。
- 实际修改：提升三类交互元素 z-index；冒烟测试固定层级、播放头宽度与 pointer-events 契约；新增两个目标尺寸的真 Chrome 几何、拖动和截图证据；跟进扩展真实 Electron DOM probe，在隔离 userData/partition 中执行 computed style、`elementFromPoint`、四类 CDP 原生 pointer drag 与空白 scrub，并由 impact/full 自动运行。

## 验证结果

| 命令/步骤 | 结果 | 耗时 | 备注 |
| --- | --- | ---: | --- |
| `node -v` | 通过 | <1s | `v24.14.0`；后续仓库命令均显式使用 Node 24 runtime PATH |
| `npm run app:status` | 通过 | <1s | Node 24；installed `7ff9aa5…`，current `62e20fe…`，contains=yes，exact=no |
| `npm run task:status` | 通过 | <1s | Node 24；共享登记可见本任务唯一 write claim |
| `npm run test:module -- timeline` | 通过 | 约 3s | Node 24；124 通过，0 失败，覆盖现有错误 pointerId、pointercancel、lost capture 会话语义 |
| `npm run test:module -- robustness` | 通过 | 约 25s | Node 24；23 通过，0 失败 |
| `npm run test:i18n` | 通过 | <1s | Node 24；21 通过，0 失败，无新增运行时文案 |
| 真 Chrome 1316×768 | 通过 | — | 四类对象同 x 命中自身并真实拖动；空白 lane/ruler scrub；console error 0 |
| 真 Chrome 1440×900 | 通过 | — | 四类对象同 x 命中自身并真实拖动；空白 lane/ruler scrub；console error 0 |
| `npm run test:impact -- --base 62e20fe29066c4fa83d6354e5baacd8b099d2e2e --module timeline` | 通过 | 约 36s | Node 24；app 892、foundation 93、coordination 31、i18n 21、project-input Web/Electron、Web 10+14 全通过 |
| `npm run test:full` | 通过 | 约 35s | Node 24；app 892、project-input Web/Electron、Web 10+14、desktop 43、local install 36+13、foundation 93、coordination 31、i18n 21、launcher 11 全通过 |
| `npm run test:project-input`（跟进） | 通过 | 约 2s | Node 24；Web/Electron 输入探针通过，timeline 真 Chromium computed stacking、elementFromPoint、四类真实 pointer drag 与空白 scrub 通过 |
| `npm run test:module -- timeline`（跟进） | 通过 | 约 3s | Node 24；124 通过，0 失败 |
| `npm run test:module -- robustness`（跟进） | 通过 | 约 25s | Node 24；23 通过，0 失败 |
| `npm run test:i18n`（跟进） | 通过 | <1s | Node 24；21 通过，0 失败 |
| `npm run test:impact -- --base 62e20fe29066c4fa83d6354e5baacd8b099d2e2e --module timeline`（跟进） | 通过 | 约 38s | Node 24；识别 9 个文件，自动执行并通过新增 timeline Chromium DOM probe，其他 app/foundation/project-input/Web 全通过 |
| `npm run test:full`（跟进） | 通过 | 约 40s | Node 24；app 892，新增 timeline Chromium DOM probe，Web 10+14、desktop 43、local install 36+13、foundation/coordination/i18n/launcher 全通过 |

固定 App installed source：7ff9aa583b4e51fb4d888aa1815792b747d275d7

固定 App 人工启动结果：不适用；仅开发预览，禁止更新或启动交付流程。

## 未覆盖与后续

- 本任务未在 Electron 固定 App 中人工检查，因为委派阶段明确为开发预览；由 00 集成并在正式交付阶段统一执行。
- CSS 局部层级修复不改变数据、插值、history/autosave 或 pointer 会话代码；残余风险仅是未来新增更高 z-index 的时间轴交互元素需继续遵守同一层级契约。
- 由 00 集成后释放 claim，并决定最终固定 App 交付时机。

## 交接

- 最终提交：`438eff3ef745010242fe15ad6c92d9f38a23a0c8`，另有聚焦自动布局探针跟进提交（精确哈希见交接消息）
- PR：无（仓库无 remote）
- 工作区状态：提交后 clean
- 下一步：00 精确集成任务提交，运行集成回归后释放 claim
