# 03.10 保存状态响应式可见性证据

日期：2026-07-16

测试基线：`62e20fe29066c4fa83d6354e5baacd8b099d2e2e`

被测实现：`6c534275c3b6c47cdb5dcb9b81612f9696404e21`

实现分支：`fix/03.10-save-state-responsive-visibility`

## 结论

- 真 Chrome 与隔离 Electron 验证时的工作树业务内容已提交为 `6c534275c3b6c47cdb5dcb9b81612f9696404e21`；`62e20fe…` 仅是 impact 比较和任务起点基线，不是被测实现来源。
- 唯一 `#saveState` 已移入现有底部 `#statusBar[role=status]` 的中央弹性区域；没有复制节点或新增状态同步。
- 真 Chrome 1316×768 与 1440×900 中，自动保存状态均为 `display:block`、`visibility:visible` 且 rect 为 104.19×14 CSS px。
- 1316×768 普通态与导演专注态均可见；导演专注截图见 `1316x768.png`。1440×900 普通态截图见 `1440x900.png`。
- 两个目标尺寸的 document、顶栏、状态栏横向 overflow 均为 0；1316px 顶栏可见动作均保持非零 29px 以上命中尺寸。
- 真 Chrome 1316×768 已触发自动保存与浏览器项目保存，状态分别显示自动保存时间和项目文件已保存反馈。
- 隔离 Electron 从当前工作树加载，在约 1229×768 内容窗口通过原生保存面板真实保存去敏临时项目；辅助功能树完整读取 `项目已保存 · <temporary-dir>/prevision-0310-responsive-check.previz.json`。临时项目与隔离 userData 已删除。
- 760×768 中保存状态仍为 104.19×14 CSS px，状态栏 overflow 为 0；页面整体已有 86px 窄屏 overflow，本次状态节点位于其内部弹性区，没有增加状态栏 overflow，也不宣称整个导演台支持窄屏。

## 截图说明

- `1316x768.png`：真 Chrome、导演专注态；底部中央偏右可见自动保存反馈，左右栏与时间轴按既有专注语义收起。
- `1440x900.png`：真 Chrome、普通态；底部中央偏右可见自动保存反馈，左栏、属性栏与时间轴均保持正常。
- 两张文件均已复核为真实 PNG（8-bit RGB、非交错），尺寸分别为 1316×768 与 1440×900；SHA-256 分别为 `ef50086614b680b10ba8bed7648142ecfd2ad0afb1baa95c06cb3656f6387570`、`845d06a7cfa6e60f1c93ed736b2996f72bbbc1e47a4ea81eefebf309246895f3`。
- 元数据检查未发现 EXIF 版本、作者、创建器或来源字段；截图不含 EXIF 私密信息。

截图只包含仓库内置欢迎项目与去敏验证名称，不包含用户项目、真实私人路径或媒体内容。

## 覆盖与边界

- 自动测试覆盖启动状态写入、自动保存成功/降级/失败、项目保存、捕获目标与完成、录屏和导出完成链路仍写入同一节点；本任务没有改动这些业务函数。
- 真 Chrome 捕获完成状态未再次生成媒体文件；捕获路径与完整 `title` 契约由 capture 107 项执行级测试覆盖。Electron 原生项目保存提供了实际完整路径可读性证据。
- 快速开发预览不执行 `app:deliver`，不更新固定 App 或 4175 稳定预览。
