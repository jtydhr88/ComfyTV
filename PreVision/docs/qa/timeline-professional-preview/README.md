# 时间轴专业化预览版 QA

日期：2026-07-15 至 2026-07-16

分支：`feat/timeline-professional-preview`

基线：`3f5f9b4df7c90228b998381f48f869e6ec6589a5`

## 真实 Electron 检查

- 用独立用户数据目录和专用调试端口 9335 启动了任务分支的 Electron，不读写固定 App 会话。
- 系统可识别真实 Electron 窗口“预见 PreVision · AI 分镜预演”；欢迎场景、导演台与展开的专业时间轴均正常渲染，没有白屏或崩溃。
- 宿主屏幕完成了 1316 级窗口的可见性检查；CUA 产生的临时截图在有界验收被中断后未持久化，因此不伪造图片文件。
- 未在本轮重跑 1440×900 真机截图；该尺寸的面板高度、通道布局和溢出边界由 121 项 layout 回归和主应用结构断言覆盖。
- 验收后已关闭隔离 Electron，端口 9335 无监听；未运行 `app:deliver`。

## 自动交互证据

- 时间轴 114 项覆盖父轨/子轨展开、稳定空通道、软组与子键错位、父组锚点边界、相邻键防碰撞、时长重映射和删镜头前移。
- 手动 K 与 Auto Key 共用候选值管线；Auto 按 owner/channel 隔离。回归显式覆盖开启 Auto 前未超过 250ms 的手动历史先落盘，以及 250ms 内连续两次 Auto 分别撤销。
- 画布、标尺、播放头、父组、子键、旧轨道键与 UE 式数值拖动均有 pointer capture、cancel、lost capture 和 blur 幂等收尾路径；父组、子键、旧轨道、画布和时间轴高度另有真实事件分发断言，证明第二指针释放/取消/丢失捕获不会提前提交或清理第一指针的活动拖动。
- 父组边界回归先把组内 `scale` 子键拖到 `0`，再把父组向右拖过 owner 末端，验证组锚点与子键都位于 `[0, ownerLimit]` 且相对错位不变。
- 位移/旋转/缩放/高度采样复用现有碰撞与地形安全路径；挂载根通道清理、摄影机锁定、独立关节与程序步态共存有专项断言。
- sidecar 不进入 `stageToData()`、project v5 或 autosave；撤销时先恢复 sidecar 再重建场景，防止撤销前采样污染舞台。

## 未执行的交付

本任务是开发预览，没有运行 `app:deliver`，没有更新固定 App，也没有公开部署。
